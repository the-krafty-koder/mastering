# Compiling a C program

- Obtain the source code either by ftp or http
- Run

```
./configure       // script that analyzes the build environment, generates a Makefile
make
```

- A special make target called install installs the final compiled product into a system directory (/usr/local/bin)
